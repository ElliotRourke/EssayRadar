The program works by firstly saving a document in a "data" folder in the application.
The program extracts the file name and amends it to the end of the "data" folder path.
The program then parses the document, compares it and stores the final report.
